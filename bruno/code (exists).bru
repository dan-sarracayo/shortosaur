meta {
  name: code (exists)
  type: http
  seq: 5
}

get {
  url: {{url}}/codehere
  body: none
  auth: inherit
}

assert {
  res.status: eq 302
}

script:pre-request {
  req.setMaxRedirects(0); // Stop Redirects
  req.setUrl(bru.getEnvVar('url')+"/"+bru.getVar('fresh'))
}
