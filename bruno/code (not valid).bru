meta {
  name: code (not valid)
  type: http
  seq: 6
}

get {
  url: {{url}}/inval
  body: none
  auth: inherit
}

assert {
  res.status: eq 200
  res.body: contains Invalid Link
}

script:pre-request {
  req.setMaxRedirects(0); // Stop Redirects
}
